#include<stdio.h>

int main(){
//          for (int a=0; a<10;a++){
//    printf("The value of a is %d\n", a+1);
//     }
    

    for(int i =100;i>=1; i =i-1)    // for changing the value of i minus soming value are decreasing in fast way
{
    printf("The value of i is %d\n", i--);  // for rewasa even number we are use i-- 
}                                           // for rewasa order only number we are use i only     
    return 0;
} //   i +1 we are use short form i++ 