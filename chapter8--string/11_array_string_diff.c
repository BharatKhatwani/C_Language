#include <stdio.h>
     void printString(char arr[]);                                               //#include <string.h>


int main()
{                                        // jitana word uthna length ki value
    char *canChange = "bharat";
   puts(canChange);
   canChange = "hello";
   puts(canChange);
   
char cannotchange[] = "hello World";             // now array ,me hum modification nhi kr skta ha 
// puts(cannotChange);
// cannotChange = "hello";










    // int a = strlen(str);
    // printf("The length of string st is %d", a);
    return 0;
}