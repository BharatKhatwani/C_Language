#include<stdio.h>

int main(){
    FILE *ptr;
  // ptr = fopen("sample.txt", "r");                          //--> for reading the file
   ptr = fopen("pawan.txt", "w");                           //--> for writing the file
                                                               // jo jo likh dega vo hi answer bolta ha
    return 0;
}